# ================================================================
# requirements.txt
# Python 3.11+
# ================================================================

# Google Gemini API (nouveau SDK — remplace google-generativeai déprécié)
google-genai>=1.0.0

# Anthropic Claude API
anthropic>=0.34.0

# Telegram Bot
python-telegram-bot>=20.0

# Playwright (scraping Pinterest)
playwright>=1.40.0

# Image manipulation
Pillow>=10.0

# Segmentation automatique de personnage — workflow inpainting (local, sans API)
rembg>=2.0.0

# HTTP requests (Meta Graph API)
requests>=2.31

# Variables d'environnement
python-dotenv>=1.0

# Extraction de frames vidéo
# imageio-ffmpeg embarque les binaires ffmpeg — fonctionne sur Windows sans install système
# Sur Linux/VPS, ffmpeg système (apt) est utilisé en priorité s'il est dans le PATH
imageio-ffmpeg>=0.4.9
ffmpeg-python>=0.2.0

# JWT pour l'authentification Kling AI
PyJWT>=2.0.0

# ================================================================
# Post-install :
#   playwright install chromium
#   sudo apt install ffmpeg  (Linux/VPS)
# ================================================================
